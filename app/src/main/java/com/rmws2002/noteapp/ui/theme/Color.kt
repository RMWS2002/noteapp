package com.rmws2002.noteapp.ui.theme

import androidx.compose.ui.graphics.Color

// ── Warm Amber Palette (MD3 Token-driven) ──
// Primary: warm amber-gold — warmer than cold blue, fits a notebook/journal aesthetic
val Primary = Color(0xFFB8923C)
val PrimaryDark = Color(0xFF8B6A2E)
val PrimaryLight = Color(0xFFF5ECD7)

// Backgrounds — warm white base
val BgLight = Color(0xFFFBFAF7)
val BgDark = Color(0xFF111110)

// Surfaces — clean white / dark card
val SurfaceLight = Color(0xFFFFFFFF)
val SurfaceDark = Color(0xFF1C1C1E)

val SurfaceVariantLight = Color(0xFFF0EFEA)
val SurfaceVariantDark = Color(0xFF2C2C2E)

// On-surface text — deep brown-black (softer than pure #000)
val OnSurfaceLight = Color(0xFF1D1B18)
val OnSurfaceDark = Color(0xFFF0EFEA)

// Outline
val OutlineLight = Color(0xFFD6D3CD)
val OutlineDark = Color(0xFF3A3A3C)

// Semantic
val Danger = Color(0xFFDC3545)
val Success = Color(0xFF2E7D32)
val Warning = Color(0xFFE8A840)

// Tag accent colors (soft pastels)
val TagBlue = Color(0xFF6B9FFF)
val TagGreen = Color(0xFF66D89A)
val TagOrange = Color(0xFFFFB066)
val TagPurple = Color(0xFFAF8CFF)
val TagPink = Color(0xFFFF8BA7)
val TagYellow = Color(0xFFFFD666)
